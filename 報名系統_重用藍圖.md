# 活動報名・查詢系統 — 重用藍圖 / 開發範本

> 來源：2026 屏東西岐城報名後台。下次要做類似的「活動報名＋車次/座位查詢」系統，照這份套用即可。
> repo 範例：`peien1013/xiqicheng-2026`（GitHub Pages）

---

## 一、系統架構（三層）

| 層 | 用什麼 | 角色 |
|---|---|---|
| 前台 | 單一 `index.html`（放 GitHub Pages） | 報名表＋查詢畫面；用 JSONP 呼叫後端 |
| 後端 | Google Apps Script（Web App，doGet/doPost） | 邏輯、寫資料、產生報表 |
| 資料 | Google 試算表（多張工作表） | 所有資料的來源（總表為主） |

前台用 `GOOGLE_SCRIPT_URL`（Apps Script 的 `/exec`）連後端；後端用 JSONP 回傳。

---

## 二、Google 試算表結構（欄位順序＝程式用位置讀，別亂調）

- **活動設定**：`設定項目 | 設定內容 | 備註`（key-value）
  - 必備列：活動名稱、活動日期、前台說明、**報名狀態**（填「開放」才可報名）、**每台車保留名額**（預設值）、各項費用、地址…
- **行程表**：`排序 | 時間 | 行程內容`
- **上車地點**：`排序 | 上車地點 | 出發時間 | 車次 | 集合地點 | 台數 | 每台座位 | 是否自行開車 | 是否啟用`
  - 「車次」欄填如 `3.12車` → 代表 3車、12車屬於這個上車地點
- **車次資料**：`車次 | 車長 | 副車長 | 車號 | 司機 | 司機電話 | 保留名額`
  - **這張表的「列順序」＝車次顯示順序**（可自由調整、自行開車也排這裡）
  - 各車保留名額可各自設定（例：一般 2、熱門車 5、自行開車 0）
- **總表**（報名資料，主檔）：`時間 | 姓名 | 車次 | 上車地點 | 出發時間 | 身分`
  - 報名一筆一列；自動依車次分組＋交替底色；重複姓名自動上色
- **（選單自動產生，不用手建）**：各車次名單、統計、車次總覽、用餐桌次

---

## 三、前台功能特色（可挑著用）

- **兩按鈕切換版面**：「報名登記 / 查詢」，預設都收合，點了才展開（互斥）
- **逐步顯示（reveal）**：選了車次才出現填姓名；填了才出現下一步 → 畫面乾淨
- **報名流程**：① 選車次（顯示 地點＋車號＋車長，**額滿自動變灰擋選**）→ ② 填姓名（一行一個、可多人）→ ③ 確認身分（**大人預設、小孩自己點改**）→ ④ 送出
- **右上 LIVE 狀態徽章**：開放＝紅點閃動「報名開放中」；關閉＝灰點「報名已關閉」
  - 狀態文字可由後台「報名狀態」格編輯；**抓不到後端時顯示「確認中…」，不會誤顯示開放**
- **摺疊資訊區**：行程／費用／注意事項收進一個摺疊塊；行程每站可附地址（小字）
- **查詢車次**：輸入姓名 → 顯示**整台車表**（車長／副車長／車號／司機／電話＋名單**4 人一排**＋小孩標註＋自己的位子高亮）
- **各車次名單**：可摺疊；列出車次按鈕（依車次資料順序），點了才秀該車名單

---

## 四、後端功能特色

- **動態車次選項**（`getBusOptions_`）：只列出「已填車長」的車；每台車上限＝每台座位−保留名額；額滿回 `full`
- **大人／小孩身分**：前台逐人選，寫入總表「身分」欄
- **重複報名**：送出時偵測同名 → 前台提示；總表「姓名」欄**條件式格式自動上色**
- **總表自動整理**：每次報名後依車次分組排序＋交替底色（也可選單手動重整）
- **報名開關**：活動設定「報名狀態」；前後端雙重檢查
- **自訂選單「報名系統」**：產生各車次名單／統計／車次總覽／用餐桌次、整理總表、**一鍵重建總表＋車次資料**、重建後台工作表
- **報表（皆即時公式或一鍵產生）**：
  - 各車次名單：車表格式（每車一塊、名單 4 人一排）
  - 統計：各車人數＋區域小計（高雄/台南/屏東 自動、台中手填）＋總人數
  - 車次總覽 / 用餐桌次：車長｜搭車人數(自動)｜實到人數(手填)｜桌號(每車4桌 1.2.3.4)｜備註/用餐(手填)｜合計
- **API action**：`status`（活動＋車次選項）、`submit`、`query`（依姓名）、`roster`（全部名單＋車次資料）

---

## 五、踩過的雷 / 設計守則（重要！）

1. **欄位是用「位置(index)」讀的** → 新增欄位**放最後**最安全；要插中間/調順序，必須**同步改所有 `row[n]` 與 COUNTIF 的欄位字母**。
2. **既有工作表不會自動長新欄位** → 改了結構要「**刪掉重建**」（做一個一鍵重建函式最方便）。
3. **報表/工作表是「按選單才產生」**，不是自動建；改了 `Code.gs` 要**重新貼上＋（Web 行為的話）重新部署**。
4. **重新部署要用「管理部署作業→編輯→新版本」**，網址才不會變（按「新增部署」會生新網址，前台就要換）。
5. **試算表的時間欄是日期物件** → 前端要 `formatTime` 轉成 HH:MM，否則顯示「Sat Dec 30 1899…」。
6. **報名狀態**：前台預設別假設「開放」；抓不到就顯示「確認中」，避免快取/斷線誤導。
7. **快取**：改了 index.html，使用者要 **Ctrl+Shift+R 或無痕** 才看得到（GitHub Pages＋瀏覽器都會快取）。

---

## 六、從零複製一套的快速清單

1. [ ] 新建 Google 試算表 →「擴充功能 → Apps Script」
2. [ ] 貼上 `Code.gs`，執行一次 `setupSheet`（建出所有後台工作表）
3. [ ] **部署 → 新增部署 → 類型 Web 應用程式 → 執行身分：我，存取：任何人** → 複製 `/exec` 網址
4. [ ] `index.html` 把 `GOOGLE_SCRIPT_URL` 換成上面的網址
5. [ ] 建 GitHub repo，放 `index.html` → Settings → Pages 開啟（main 分支）
6. [ ] 填資料：活動設定（日期/費用/報名狀態）、上車地點、車次資料（**車長**＝車次才會出現）
7. [ ] 試算表「報名系統」選單 → 產生各車次名單（一次生成各報表）
8. [ ] 測試：報名一筆 → 看總表分組上色、查詢車表、各報表數字

> 改 `Code.gs` 後 → 重新部署（新版本）；改 `index.html` 後 → 推 GitHub＋強制重新整理。

---

## 七、可再擴充的點（本系統已預留思路）

- **候補**：額滿車可候補 → 寫進獨立「候補名單」表（不佔正取），有空位手動遞補
- **費用試算**：依大人/小孩數×費用自動算金額
- **匯出 / 列印**：各車次表、用餐桌次可直接印給各車車長
- **通知**：報名成功顯示安排；候補由承辦人手動聯絡
